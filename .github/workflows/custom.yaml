name : sal Custom Action CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Hello World custom action
        uses: custom_actions/sal-custom-action@main
        with:
          who to greet: "World"